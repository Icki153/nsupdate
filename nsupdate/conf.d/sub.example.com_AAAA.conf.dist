# Required zone and fully qualified record name.
MAIN_DOMAIN="example.com"
DOMAIN="sub.example.com"

# Optional per-record overrides.
RECORD_TYPE="AAAA"
RECORD_TTL="300"

# INWX_USER="different-api-user"
# INWX_PASSWORD="different-api-password"
# INWX_SHARED_SECRET="DIFFERENT_BASE32_SECRET"

# Optional command used instead of the detected public WAN address.
# WAN_IP_COMMAND="ip -6 addr show dev eth0 scope global | awk '/inet6/{print $2}' | cut -d/ -f1 | head -n1"
